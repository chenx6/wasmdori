{
  "devDependencies": {
    "vite": "^2.3.3"
  },
  "dependencies": {
    "hyperapp": "^2.0.18"
  },
  "scripts": {
    "dev": "vite",
    "build": "cp libs/*.wasm public && vite build",
    "serve": "vite preview"
  }
}
